{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Rust dependencies",
      "matchManagers": ["cargo"],
      "commitMessage": "chore(deps): bump Rust dependency {{depName}} to {{newVersion}}"
    },
    {
      "description": "Nix Flake inputs",
      "matchManagers": ["nix"],
      "commitMessage": "chore(deps): bump Nix Flake input {{depName}} to {{newDigest}}"
    }
  ],
  "nix": {
    "enabled": true
  },
  "rust-toolchain": {
    "enabled": false
  }
}
