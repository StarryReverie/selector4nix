[package]
name = "selector4nix-db"
version = "0.0.0"
edition = "2024"

[dependencies]
selector4nix-workspace-hack = { path = "../selector4nix-workspace-hack" }

anyhow = { workspace = true }
redb = { workspace = true }
tokio = { workspace = true }
